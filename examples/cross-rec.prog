/**
 * How many stacks?
 */
void foo() {
	bool f = *;
	
	if (*)
		foo();
	while (f) {
	}
	if (!f)
		bar();
}

void bar() {
	bool b = *;
	if (*)
		bar();
	while (!b) {
	}
	if (b)
		foo();
}

void main() {
	create_thread(&foo);
	create_thread(&bar);
}